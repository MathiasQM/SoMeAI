// import { getFirestore, doc, updateDoc } from 'firebase/firestore';


// export async function updateAgeRange(personaId, ageRange, minOrMax) {
    // const db = getFirestore();
//   const personaRef = doc(db, 'Personas', personaId);
//   let targetField = `targetAudience.demographics.ageRange`;
  
//   console.log(`${ageRange[minOrMax === 'min' ? 0 : 1]} (${minOrMax})`);

//   await updateDoc(personaRef, {
//     [targetField]: ageRange,
//   });
// }